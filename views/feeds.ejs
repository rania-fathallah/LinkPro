<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <!-- Favicons -->
  <link href="/img/favicon.png" rel="icon">
  <link href="/img/apple-touch-icon.png" rel="apple-touch-icon">

  <!-- Google Fonts -->
  <link
    href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,600,600i,700,700i|Raleway:300,300i,400,400i,500,500i,600,600i,700,700i|Poppins:300,300i,400,400i,500,500i,600,600i,700,700i"
    rel="stylesheet">

  <!-- Vendor CSS Files -->
  <link href="/vendor/aos/aos.css" rel="stylesheet">
  <link href="/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
  <link href="/vendor/bootstrap-icons/bootstrap-icons.css" rel="stylesheet">
  <link href="/vendor/boxicons/css/boxicons.min.css" rel="stylesheet">
  <link href="/vendor/glightbox/css/glightbox.min.css" rel="stylesheet">
  <link href="/vendor/swiper/swiper-bundle.min.css" rel="stylesheet">
  <!-- Template Main CSS File -->
  <link href="/css/style.css" rel="stylesheet">

  <style>
    body {
      background-color: #F5EFE7;
      display: flex;
      flex-direction: column;
      /* Stack child elements vertically */
      height: 100vh;
      /* 100% of the viewport height */
      margin: 0;
    }

    header {
      height: 50px;
    }



    main {
      flex: 2;
      display: flex;
    }

    aside {
      width: 300px;
      background-color: #D9D9D9;
      color: #fff;
      padding: 20px;
    }

    footer {
      background-color: #D9D9D9;
      color: #fff;
      text-align: center;
      padding: 10px;
    }

    form {
      max-width: 600px;
      margin-top: 30px;
    }

    #textarea-div {
      display: flex;
      align-items: center;
      margin-bottom: 10px;
    }

    #profile-ph {
      border-radius: 50%;
      height: 60px;
      width: 60px;
      float: inline-start;
      margin-right: 15px;
      margin-bottom: 20px;
    }

    textarea {
      width: 600px;
      height: 80px;
      border: 3px solid #cccccc;
      padding: 5px;
      font-family: Tahoma, sans-serif;
      background-image: url(bg.gif);
      background-position: bottom right;
      background-repeat: no-repeat;
      border-radius: 10px;

    }

    input[type="file"] {
      display: none;
      /* Hide the default file input */
    }

    #pdf-btn {
      display: flex;
      height: 30px;
      width: 30px;
    }

    #pdf-btn:hover {
      background-color: #f3e3ce;
      cursor: pointer;
    }

    #video-btn {
      display: flex;
      height: 30px;
      width: 30px;
      border-radius: 10px;
    }

    #video-btn:hover {
      background-color: #f3e3ce;
      cursor: pointer;
    }

    #photo-btn {
      display: flex;
      height: 30px;
      width: 30px;
      border-radius: 100px;
    }

    #photo-btn:hover {
      background-color: #f3e3ce;
      cursor: pointer;
    }

    .custom-file-upload-label {
      background-size: cover;
      background-position: center;
      height: 200px;
      width: 200px;
      display: inline-block;
      cursor: pointer;
      color: transparent;
    }

    #content {
      display: inline-flex;
      cursor: pointer;
      border: none;
      gap: 20px;
      margin-left: 90px;
    }

    input[type="submit"] {
      background-color: #213555;
      color: white;
      padding: 4px 8px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      float: right;
    }

    input[type="submit"]:hover {
      background-color: #4F709C;

    }

    #posts {
      max-width: 600px;
      margin-top: 30px;
    }

    .post {
      max-width: 600px;
      margin: 0 auto;
      background-color: #fff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      margin-bottom: 20px;
    }

    .post-content {
      margin-bottom: 20px;
    }

    .post img {
      max-width: 100%;
      border-radius: 8px;
    }

    .post video {
      max-width: 100%;
      border-radius: 8px;
    }

    .like-btn {
      display: none;
    }

    .like--btn {
      max-width: 105px;
      max-height: 50px;
      cursor: pointer;
      margin-top: 10px;
    }

    .post-profile-photo {
      border-radius: 50%;
      height: 60px;
      width: 60px;
      float: inline-start;
      margin-right: 15px;
      margin-bottom: 20px;
    }

    .post-profile-name {
      height: 60px;
      width: 60px;
      font-weight: bold;

      color: #040440;
      font-size: 20px;
    }

    .profile-info {
      margin-bottom: 20px;
      margin-top: 20px;


    }

    .profile-photo {
      border-radius: 20%;
      height: 80px;
      width: 80px;
      float: inline-start;
      margin-right: 10px;
      margin-bottom: 20px;
      margin-top: 5px;
      display: block;
      float: left;
    }

    .profile-name {
      font-weight: bold;
      color: #4F709C;
      font-size: 28px;
      display: block;
      clear: left;
    }

    .profile-buttons {
      margin-top: 50px;
    }

    .profile-btn {
      background-color: #3498db;
      color: #fff;
      padding: 8px 12px;
      border: none;
      border-radius: 4px;
      margin-right: 10px;
      cursor: pointer;
    }

    .search-bar {
      display: flex;
      align-items: center;
    }

    .search-bar input {
      padding: 8px;
      margin-right: 5px;
      margin-left: 5px;
      border: none;
      border-radius: 4px;
    }

    .search-bar button {
      background-color: #cccccc;
      color: #fff;
      border: none;
      padding: 8px;
      border-radius: 4px;
      cursor: pointer;
    }
  </style>
  <script>
    async function likePost(button) {
      const postId = button.dataset.postId;
      try {
        const response = await fetch(`/like/${postId}`, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          // You might want to include additional data in the request body if needed
          body: JSON.stringify({}),
        });

        const updatedPost = await response.json();
        // Log the server response
        console.log(updatedPost);

        // Handle the updated post data as needed (e.g., update the UI)
      } catch (error) {
        console.error('Error liking post:', error);
      }
    }  
  </script>
  <base href="/">
</head>

<body>
  <!-- ======= Header ======= -->
  <header id="header" class="fixed-top d-flex align-items-center">
    <div class="container d-flex align-items-center justify-content-between">

      <div class="logo">
        <a href="/ent/about_us"><img src="/img/logo.png" alt="" class="img-fluid"></a>
      </div>

      <nav id="navbar" class="navbar">
        <!-- Search Bar -->
        <div class="search-bar">
          <input type="text" placeholder="Search">
          <button type="submit"><i class="bi bi-search"></i></button>
        </div>



        <ul>
          <li><a class="active" href="/">Home</a></li>
          <li><a class="inactive" href="/ent/about_us">About_us</a></li>
          <li><a class="inactive" href="/ent/job_search">Job Search</a></li>
          <li><a class="inactive" href="/ent/">Enterprise</a></li>
          <li><a class="getstarted" href="/user/portfolio-details/<%= user.email %>">Profile</a></li>
        </ul>

      </nav><!-- .navbar -->

    </div>
  </header><!-- End Header -->


  <main id="main">



    <aside>
      <!-- Sidebar content goes here -->
      <div class="profile-info">
        <img class="profile-photo" src="data:image/jpeg;base64,<%= user.image.toString('base64') %>"
          alt="Profile Photo">
        <span class="profile-name">
          <%= user.nom %>
        </span>
        <span class="profile-name">
          <%= user.prenom %>
        </span>
      </div>

    </aside>







    <!-- Your existing content goes here -->
    <div class="container">


      <form action="/feed/submit-post" id="label-textarea" method="post" enctype="multipart/form-data">

        <div id="textarea-div">
          <label for="postContent"><img id="profile-ph" src="./img/about.jpg"></label>
          <textarea id="postContent" name="postContent" placeholder="What are you thinking about..."></textarea>
        </div>

        <div id="content">
          <label for="photo" id="photo-btn" class="custom-file-upload"><img
              src="/img/1200px-Picture_icon_BLACK.svg.png"></label>
          <input type="file" id="photo" name="photo" accept="image/*">

          <label for="pdf" id="pdf-btn"><img src="/img/pdf-outline.png"></label>
          <input type="file" id="pdf" name="pdf" accept=".pdf">

          <label for="video" id="video-btn" class="custom-file-upload"><img src="/img/video-icon.png"></label>
          <input type="file" id="video" name="video" accept="video/*">

        </div>
        <input id='share-btn' type="submit" value="Share Post">
      </form>


      <div id="posts">


        <% posts.forEach(post=> { %>
          <div class="post" data-post-id="<%= post._id %>">
            <div class="post-profile-info">
              <img src="data:image/jpeg;base64,<%= post.author.image.toString('base64') %>" alt="Profile Photo"
                class="post-profile-photo">
              <span class="post-profile-name">
                <%= post.author.nom %>
              </span>
            </div>
            <div class="post-content">
              <p>
                <%= post.content %>
              </p>
            </div>
            <!-- Display PDF content -->
            <% if (post.content_pdf) { %>
              <a href="<%= `data:${post.content_pdf.type};base64,${post.content_pdf}` %>"
                download="pdf_file.pdf">Download PDF</a>
              <% } %>

                <!-- Display Photo content -->
                <% if (post.content_photo ) { %>
                  <img src="<%= `data:${post.content_photo.type};base64,${post.content_photo}` %>" alt="Post Photo">
                  <% } %>


                    <!-- Display Video content -->
                    <% if (post.content_video ) { %>
                      <!-- Add video player here based on your implementation -->
                      <video controls>
                        <source src="<%= `data:${post.content_video.type};base64,${post.content_video}` %>"
                          type="<%= post.content_video.type %>">
                        Your browser does not support the video tag.
                      </video>
                      <% } %>


                        <!-- Like Button -->
                        <label for="like-btn" class="like--btn"><img src="/img/like1.png"></label>
                        <button class="like-btn" data-post-id="<%= post._id %>" onclick="likePost(this)">Like</button>
                        <span class="like-count">
                          <%= post.like.count %> likes
                        </span>



          </div>
          <% }); %>
      </div>



    </div>


  </main>


  <footer>
    &copy; 2023 Social Media Page
  </footer>
</body>



</html>